<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.learningsupport_argame">
    <!--前台服务权限-->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".FeedbackModel.MissionAccomplishActivity"></activity>

        <activity android:name=".TaskList_Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ARModel.ScanModel" />
        <activity android:name=".ARModel.PutModel" />
        <activity android:name=".FeedbackModel.FeedbackDetailsActivity" />

        <activity android:name=".MainActivity" />

        <activity android:name=".GameActivity"/>

        <activity android:name="com.example.Production.UnityPlayerActivity"/>
        <activity android:name=".TaskActivity" />
        <activity android:name=".FriendList_Main" />

        <meta-data
            android:name="com.google.ar.core"
            android:value="required" />

        <service android:name=".MonitorModel.MonitorService" />

    </application>

    <!-- Both "AR Optional" and "AR Required" apps require CAMERA permission. -->
    <uses-feature android:name="android.hardware.camera.ar" />
    <!-- Indicates that app requires ARCore ("AR Required"). Ensures app is only
         visible in the Google Play Store on devices that support ARCore.
         For "AR Optional" apps remove this line. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION" />
    <uses-permission android:name="android.permission.ACCESS_DOWNLOAD_MANAGER" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

</manifest>